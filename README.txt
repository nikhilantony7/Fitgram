1. Navigate to the project directory.
2. Start MongoDB with the custom database path:
   mongod --dbpath data/db
3. Open a new terminal, navigate to the project directory, and start the Node.js server:
   node server.js
4. Open a browser and visit: http://localhost:3000
